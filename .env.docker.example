# Kopiere diese Datei nach .env und passe die Werte an:
#   cp .env.docker.example .env

# Pflichtfeld: gemeinsames App-Passwort (mindestens 4 Zeichen)
APP_PASSWORD=hierPasswortEintragen

# Optionaler Port fuer das Frontend (Standard: 80)
# CLIENT_PORT=8080
